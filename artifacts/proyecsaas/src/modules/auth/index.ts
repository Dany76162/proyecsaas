import type { AppModule } from "@/modules/types";

export const authModule: AppModule = {
  key: "auth",
  label: "Auth",
  description: "Authentication and authorization boundaries.",
  workspacePath: null,
};
